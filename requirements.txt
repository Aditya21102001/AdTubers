# AdTubers — Python dependencies (Python 3.11+, tested on 3.13)
# Install with:  pip install -r requirements.txt

# --- Core app ---
Django==5.2.15
Pillow==12.3.0
django-allauth[socialaccount]==65.18.0
django-ckeditor==6.7.3
djangocms-admin-style==3.3.1

# --- Production / deployment ---
gunicorn==26.0.0            # WSGI server (Linux hosts, e.g. Render)
whitenoise==6.12.0          # serve static files from the app process
dj-database-url==3.1.2      # read DATABASE_URL (Render/Heroku-style)
psycopg[binary]==3.3.4      # PostgreSQL driver (used in production)
